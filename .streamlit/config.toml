# Local development: use 8503 (8501 = AML knowledge base, 8502 = other app).
# Streamlit Community Cloud ignores server.port and always serves on 8501.

[server]
port = 8503
# Match policy_validation.MAX_UPLOAD_BYTES (1_000_000 bytes ≈ 1 MB).
maxUploadSize = 1
