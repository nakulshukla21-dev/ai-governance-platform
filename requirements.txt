# Production / Streamlit Cloud runtime (Linux-compatible).
# pywin32 is intentionally omitted — the mcp package pulls it in only on Windows
# (sys_platform == "win32"). Streamlit Cloud will not install pywin32.

anthropic>=0.40.0
python-dotenv>=1.0.0
mcp>=1.24.0
fastmcp>=2.0.0
streamlit>=1.28.0
anyio>=4.0.0
jsonschema>=4.20.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pypdf>=4.0.0
